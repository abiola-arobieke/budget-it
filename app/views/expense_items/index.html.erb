<h1>ExpenseItems#index</h1>
<p>Find me in app/views/expense_items/index.html.erb</p>
<p>Find me in app/views/categories/index.html.erb</p>
<ul>
  <% @expense_items.each do |item| %>
      <li class="d-flex">
        <div class="d-flex spc-btw m-100 f-end p-1 wrap bg">
        <%= link_to category_path(item.id) do %>
          <h2><%= item.name %></h2>
          <h2><%= item.author.name %></h2>
          <h2><%= item.amount %></h2>
        </div>
        <% end %>
      </li>
  <% end %>
  </ul>
  <%= link_to 'Add Expense Item', new_category_path %>